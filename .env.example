# Local AppServ PHP API (default when unset)
VITE_API_BASE_URL=http://localhost/building29/api
VITE_DEMO_MODE=false

# Set to true ONLY to browse the in-browser demo dataset without a PHP server
# VITE_DEMO_MODE=true
